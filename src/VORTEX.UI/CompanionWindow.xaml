<Window x:Class="VORTEX.UI.CompanionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:VORTEX.UI"
        Title="Companion" Height="100" Width="100"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    
    <Grid>
        <!-- Núcleo VORTEX em miniatura -->
        <local:VortexCore State="{Binding Status}" Width="80" Height="80" HorizontalAlignment="Center" VerticalAlignment="Center">
            <local:VortexCore.RenderTransform>
                <ScaleTransform ScaleX="0.5" ScaleY="0.5" CenterX="40" CenterY="40"/>
            </local:VortexCore.RenderTransform>
        </local:VortexCore>
        
        <!-- Menu de Contexto -->
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Abrir VORTEX" Command="{Binding OpenMainWindowCommand}"/>
                <MenuItem Header="Quick Chat" Command="{Binding OpenQuickChatCommand}"/>
                <Separator/>
                <MenuItem Header="Sair" Click="Exit_Click"/>
            </ContextMenu>
        </Grid.ContextMenu>
    </Grid>
</Window>
